# Personal Blog

一个从零搭建的静态个人博客雏形，直接打开 `index.html` 就可以浏览。

## 文件结构

- `index.html`：页面结构和示例内容
- `styles.css`：响应式样式
- `script.js`：文章渲染、搜索、标签筛选和阅读视图
- `assets/hero-workspace.png`：首页主视觉图片

## 修改内容

文章数据在 `script.js` 的 `posts` 数组里。你可以复制已有对象，替换标题、日期、标签、摘要和正文段落。
